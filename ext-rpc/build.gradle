plugins {
    id "io.quarkus.extension"// version "2.4.0.Final"
}
//apply plugin:  'io.quarkus.extensions'

quarkusExtension {
    deploymentModule = 'deployment'
}


dependencies {
//    implementation platform("io.quarkus:quarkus-bom:${quarkusPlatformVersion}")

    api "io.quarkus:quarkus-arc:${quarkusPlatformVersion}"
//    api "io.quarkus:quarkus-agroal:${quarkusPlatformVersion}"
//    api project(':mybatis')

    // maven provided
    compileOnly 'org.graalvm.nativeimage:svm:21.2.0'
//    compileOnly 'org.graalvm.compiler:compiler:21.2.0'

    annotationProcessor("io.quarkus:quarkus-extension-processor:${quarkusPlatformVersion}")
}
